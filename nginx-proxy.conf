resolver 1.1.1.1 ipv4=on ipv6=off;

server {
    listen 80;

    proxy_buffering off;
    proxy_cache off;
    proxy_request_buffering off;
    proxy_read_timeout 3600s;
    proxy_send_timeout 3600s;
    client_max_body_size 100m;

    location ~* (^|/)\.git(/|$) {
        return 404;
    }

    location = /backend/manifest.json { # serve manifest file for pwa while proxying the rest
        alias /usr/share/nginx/html/manifest.json;
        default_type application/manifest+json;

        add_header Access-Control-Allow-Origin https://chat.matthiaz.dev always;
        add_header Access-Control-Allow-Headers * always;
        add_header Access-Control-Allow-Methods "GET, OPTIONS" always;

        if ($request_method = OPTIONS) {
            return 204;
        }
    }

    location / {
        proxy_pass https://ai.hackclub.com;
        proxy_ssl_server_name on;
        proxy_set_header Host ai.hackclub.com;
        proxy_pass_request_headers on;

        proxy_hide_header Access-Control-Allow-Origin;
        proxy_hide_header Access-Control-Allow-Headers;
        proxy_hide_header Access-Control-Allow-Methods;
        proxy_hide_header Access-Control-Allow-Credentials;

        add_header Access-Control-Allow-Origin https://chat.matthiaz.dev always;
        add_header Access-Control-Allow-Headers * always;
        add_header Access-Control-Allow-Methods "GET, POST, OPTIONS" always;

        if ($request_method = OPTIONS) {
            return 204;
        }
    }
}